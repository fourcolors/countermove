# Copy to .env and fill in. Never commit real values (see .gitignore).

# Bright Data MCP (scrape_as_markdown, search_engine)
BRIGHTDATA_API_TOKEN=

# GitHub MCP (read repo, open PR against the pricing repo)
GITHUB_TOKEN=

# TrueFoundry (orchestrator + sandbox exec) - PAT
TFY_API_KEY=
